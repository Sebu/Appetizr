demo app TODO

- fix undo macros
- also add non DB , but valid, users to @account_table
- read prectab and update view
- printer view
- reEvaluate & lazy :) 
  menu do
    show_observe @c, :User   in show   render "user_list", :c => @c 
  end
- better locale support (change locale)
- speed! ( ruby1.9 + gobject for signaling )



